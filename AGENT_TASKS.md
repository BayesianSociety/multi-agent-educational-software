# Agent Tasks

## Requirements
- Keep requirement scope aligned with the Project Brief layers and constraints.
- Maintain explicit acceptance criteria and risks in `REQUIREMENTS.md`.

## Designer
- Keep level narrative and UX flow aligned with the Project Brief educational loop.
- Maintain ten JSON level definitions under `design/levels/` with deterministic layouts.

## Frontend
- Maintain deterministic block execution and active block highlighting in `/frontend`.
- Keep local progress persistence in `localStorage` and preserve keyboard accessibility.

## Backend
- Maintain backend API behavior and deterministic level loading in `/backend`.
- Keep Prisma schema and SQLite configuration aligned with the documented architecture.

## QA
- Keep deterministic tests passing through the documented `TEST.md` command.
- Add or update tests when execution rules, level data, or API behavior changes.
